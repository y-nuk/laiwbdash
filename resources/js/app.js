// Bootstrap 5 JS + Popper（dropdown / modal / collapse 等）
import * as bootstrap from 'bootstrap';
window.bootstrap = bootstrap;

// Bootstrap Icons CSS（SCSS から import すると Vite がフォントを解決できないため app.js から）
import 'bootstrap-icons/font/bootstrap-icons.css';
